class Add:
	def __call__(self, a, b):
		return a + b

class Subtract:
	def __call__(self, a, b):
		return a - b

class Multiply:
	def __call__(self, a, b):
		return a * b

class Divide:
	def __call__(self, a, b):
		return a / b

